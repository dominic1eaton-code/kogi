[package]
name = "kogi-client"
version = "0.1.0"
edition = "2021"

[lib]
name = "kogi_client"
path = "src/lib.rs"

[dependencies]
reqwest = { version = "0.12", features = ["blocking", "json"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
urlencoding = "2.1"
